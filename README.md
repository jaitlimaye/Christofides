# Christofides
Christofides Algorithm

makefile is provided

execute using ./run <tests/t01.in to run test 1

Note: Since the paths can be different everytime the algorithm is run, paths are not shown in the output file. It can be turned on by seting the DEBUG variable to true in the main.cpp file 